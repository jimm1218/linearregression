# 用於生成 Streamlit 線性迴歸模擬器的 Prompt

此檔案記錄了用於生成 `streamlit_app.py` 互動式應用程式的原始 Prompt。保留此 Prompt 有助於未來對程式碼進行迭代、除錯或擴展功能。

---

請扮演一位資深的資料科學家與 Python 專家，為我撰寫一份**完整的 Streamlit 應用程式**，用以動態、互動式地展示線性迴歸分析。請包含清晰的中文註解，並實現以下功能：

**1. 介面佈局與互動控制 (UI/UX):**

*   **主標題**: "動態線性迴歸與離群值分析模擬器"。
*   **側邊欄 (Sidebar)**:
    *   建立一個標題為 "動態參數設定" 的區塊。
    *   提供四個滑桿 (Slider) 讓使用者可以動態調整：
        *   `數據點數量 (n)`: 範圍 50 ~ 1000，預設 200。
        *   `真實斜率 (a)`: 範圍 -50 ~ 50，預設 25。
        *   `真實截距 (b)`: 範圍 0 ~ 100，預設 50。
        *   `雜訊變異數 (var)`: 範圍 0 ~ 300，預設 150。
    *   提供一個 "重新生成隨機雜訊" 的按鈕。點擊後應更換數據點的隨機分佈。
*   **主面板 (Main Panel)**:
    *   使用 `st.metric` 顯示「最佳擬合斜率」、「最佳擬合截距」及其與真實值的差異。
    *   將主畫面分為兩欄，左邊較寬用於圖表，右邊較窄用於顯示離群值數據。

**2. 核心邏輯 (Backend Logic):**

*   **數據模擬**: 根據側邊欄滑桿的值，使用公式 `y = ax + b + N(0, var)` 生成數據。
*   **平滑體驗**: 使用 `st.session_state` 管理隨機種子 (random seed)。當使用者調整 `a`, `b` 等滑桿時，數據點的相對位置應保持穩定，不應完全重新隨機生成，只有在點擊 "重新生成" 按鈕時才更換種子。
*   **模型擬合**: 對生成的數據執行 `scikit-learn` 的 `LinearRegression`。
*   **離群值分析**: 找出絕對殘差最大的前 10 個點。

**3. 視覺化與數據呈現 (Visualization & Data):**

*   **圖表 (左欄)**:
    *   使用 `matplotlib` 繪製圖表。
    *   圖表上需包含：
        1.  原始數據的散佈圖。
        2.  代表真實參數的綠色虛線 (True Line)。
        3.  模型擬合出的紅色實線 (Regression Line)。
        4.  用紅色空心圓圈特別標示出的前 10 個離群值。
    *   圖表需有標題、X/Y 軸標籤和圖例。
*   **數據表格 (右欄)**:
    *   使用 `st.dataframe` 以表格形式顯示前 10 名離群值的 X 座標、Y 座標及殘差值。